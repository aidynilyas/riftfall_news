#name: Riftfall News Daemon

#on:
  #schedule:
  #  - cron: '0 */6 * * *'
  #workflow_dispatch: {}

#jobs:
#  run:
#    permissions:
 #     contents: write
 #   runs-on: ubuntu-latest
  #  environment: SUPABASE_URL
 #   steps:
  #    - name: Checkout
  #      uses: actions/checkout@v4

   #   - name: Setup Node.js
    #    uses: actions/setup-node@v4
   #     with:
          #node-version: 20

   #   - name: Install dependencies
    #    run: npm ci

    #  - name: Run news daemon
     #   env:
    #      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
     #     SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
      #    CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
       #   CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        #  CLOUDFLARE_AI_MODEL: ${{ secrets.CLOUDFLARE_AI_MODEL != '' && secrets.CLOUDFLARE_AI_MODEL || '@cf/meta/llama-3-8b-instruct' }}
         # GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          #GITHUB_REPO: ${{ github.repository }}
          #GITHUB_BRANCH: ${{ secrets.GITHUB_BRANCH != '' && secrets.GITHUB_BRANCH || 'main' }}
          #NEWS_FILE_PATH: ${{ secrets.NEWS_FILE_PATH != '' && secrets.NEWS_FILE_PATH || 'data/news/news.json' }}
          #DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL != '' && secrets.DISCORD_WEBHOOK_URL || '' }}
          #MAX_NEWS_ITEMS: ${{ secrets.MAX_NEWS_ITEMS != '' && secrets.MAX_NEWS_ITEMS || '200' }}
        #run: npm run start
